/// menu v2's dark backing: sits at -450, BEHIND the menu_blur layer
/// (-500 blurs everything deeper), so the gaussian smooths its
/// banding while the panel above stays sharp. reads syst_menu2's
/// fold and self-destructs with it.

depth = -450;
