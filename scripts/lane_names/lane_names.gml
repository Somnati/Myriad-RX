/// @description lane_names() -> the six lanes, in the save's order (q259): order / wild / trade / faith / welcome / dread
function lane_names() {
	static _n = ["order", "wild", "trade", "faith", "welcome", "dread"];
	return _n;
}
