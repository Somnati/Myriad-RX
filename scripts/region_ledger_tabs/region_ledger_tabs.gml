/// @description region_ledger_tabs() -> the [influence] view's tabs, in order (q286): lanes / factions / people / events / memory
function region_ledger_tabs() { return ["lanes", "factions", "people", "events", "memory"]; }
