/// @description move_to(from,to,adj);
/// @param from
/// @param to
/// @param adj

function move_to(){
	
	if argument[0] != argument[1] 
	return argument[0]+((argument[1]-argument[0])/(argument[2]/delta)) 
	else return argument[0];

}